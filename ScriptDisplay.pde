class ScriptDisplay extends UIFullscreenObject {
  PGraphics scriptGraphics;
  
  ScriptDisplay(UIObject _p) {
    super(_p);
  }
  
  void drawSelf(int w, int h, PGraphics g) {
    
  }
  
  void renderScript(PGraphics g) {
    g.beginDraw();
    
    g.endDraw();
  }
}
